<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright 2017 LasLabs Inc.
     License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl). -->

<odoo>

    <template id="portal_my_home_menu_contracts" 
              name="Portal layout : contract menu entries" 
              inherit_id="portal.portal_breadcrumbs" 
              priority="25">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'contract' or contract" 
                t-attf-class="breadcrumb-item #{'active ' if not contract else ''}">
                <a t-if="contract"
                   t-attf-href="/my/contracts?{{ keep_query() }}">Contracts</a>
                <t t-else="">Contracts</t>
            </li>
            <li t-if="contract" class="breadcrumb-item active">
                <t t-esc="contract.name"/>
            </li>
        </xpath>
    </template>

    <template id="portal_my_home_contracts" 
              name="Portal My Home : contract entries" 
              inherit_id="portal.portal_my_home" 
              priority="25">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="inside">
            <t t-if="contract_count" t-call="portal.portal_docs_entry">
                <t t-set="title">Contracts</t>
                <t t-set="url" t-value="'/my/contracts'"/>
                <t t-set="count" t-value="contract_count"/>
            </t>
        </xpath>
    </template>

    <template id="portal_my_contracts" name="My Contracts">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>

            <t t-call="portal.portal_searchbar">
                <t t-set="title">Contract</t>
            </t>
            
            <t t-if="not contracts">
                <div class="alert alert-warning mt8" role="alert">
                    <p>There are currently no contracts for your account.</p>
                </div>
            </t>

            <t t-if="contracts" t-call="portal.portal_table">
                <thead>
                    <tr class="active">
                        <th>Contract #</th>
                        <th>status</th>
                        <th class="text-right">Date of Next Invoice</th>
                    </tr>
                </thead>
                <tbody>
                    <tr t-foreach="contracts" t-as="contract">
                        <td>
                            <a t-attf-href="/my/contracts/#{contract.id}?#{keep_query()}">
                                <span t-field="contract.name"/>
                            </a>
                            <!-- <a t-att-href="contract.get_portal_url()" t-att-title="contract.name">
                                <t t-esc="contract.name"/>
                            </a> -->
                        </td>
                        <td>
                            <t t-if="contract.active">
                                <span>☒</span>
                            </t>
                            <t t-if="not contract.active">
                                <span>☐</span>
                            </t>
                        </td>
                        <td class="text-right">
                            <span t-field="contract.recurring_next_date" t-options='{"widget": "date"}'/>
                        </td>
                    </tr>
                </tbody>
                <div t-if="pager" class="o_portal_pager text-center">
                    <t t-call="portal.pager" />
                </div>
            </t>
        </t>
    </template>

    <template id="portal_contract_page" name="Contract"  inherit_id="portal.portal_sidebar" primary="True">
        <xpath expr="//div[hasclass('o_portal_sidebar')]" position="inside">
            <t t-set="o_portal_fullwidth_alert" groups="sales_team.group_sale_salesman">
                <t t-call="portal.portal_back_in_edit_mode">
                    <!-- action: contract.env.ref('contract.action_customer_contract').id -->
                    <t t-set="backend_url" t-value="'/web#return_label=Website&amp;model=%s&amp;id=%s&amp;action=%s&amp;view_type=form' % (contract._name, contract.id, action)"/>
                </t>
            </t>

            <div class="row mt16 o_portal_contract_sidebar">
                <!-- Sidebar -->
                <t t-call="portal.portal_record_sidebar">
                    <t t-set="classes" t-value="'col-lg-auto d-print-none'"/>

                    <t t-set="title">
                        <h2 class="mb-0"><b t-field="contract.name" data-id="contract_name"/> </h2>
                    </t>

                    <t t-set="entries">
                        <ul class="list-group list-group-flush flex-wrap flex-row flex-lg-column">
                            <li class="list-group-item flex-grow-1">
                                <!-- TODO accept & sign, add modal etc. -->

                                <!-- Print / Download Buttons -->
                                <div class="o_download_pdf btn-toolbar flex-sm-nowrap">
                                    <div class="btn-group flex-grow-1 mr-1 mb-1">
                                        <a class="btn btn-secondary btn-block o_download_btn" t-att-href="contract.get_portal_url(report_type='pdf', download=True)" title="Download"><i class="fa fa-download"/> Download</a>
                                    </div>
                                    <div class="btn-group flex-grow-1 mb-1">
                                        <a class="btn btn-secondary btn-block o_print_btn o_portal_invoice_print" t-att-href="contract.get_portal_url(report_type='pdf')" id="print_invoice_report" title="Print" target="_blank"><i class="fa fa-print"/> Print</a>
                                    </div>
                                </div>
                            </li>

                            <!-- Navigation Menu -->
                            <li class="navspy list-group-item pl-0 flex-grow-1" t-ignore="true" role="complementary">
                                <ul class="nav flex-column bs-sidenav"></ul>
                            </li>

                            <!-- Add Info -->

                            <!-- User discussion / feedback -->
                            <li t-if="contract.user_id" class="list-group-item flex-grow-1">
                                <div class="small mb-1"><strong class="text-muted">Your Contract</strong></div>
                                <div class="row flex-nowrap">
                                    <div class="col flex-grow-0 pr-2">
                                        <img class="rounded-circle mr4 float-left o_portal_contact_img" t-if="contract.user_id.image" t-att-src="image_data_uri(contract.user_id.image)" alt="Contact"/>
                                        <img class="rounded-circle mr4 float-left o_portal_contact_img" t-if="not contract.user_id.image" src="/web/static/src/img/placeholder.png" alt="Contact"/>
                                    </div>
                                    <div class="col pl-0" style="min-width: 150px">
                                        <span t-field="contract.user_id" t-options='{"widget": "contact", "fields": ["name", "phone"], "no_marker": True}'/>
                                        <a href="#discussion" class="small"><i class="fa fa-comment"></i> Give Feedback</a>
                                    </div>
                                </div>
                            </li>
                        </ul>
                    </t>
                </t>

                <!-- Page content -->
                <div id="contract_page_content" class="col-12 col-lg justify-content-end">
                    <!-- TODO: modal relative to the actions sign and pay -->
                    <!-- TODO: modal relative to the action reject -->
                    <!-- TODO: status messages -->
                    
                    <!-- main content -->
                    <div t-attf-class="card #{'pb-5' if report_type == 'html' else ''}">
                        <div t-call="website_portal_contract.contract_content" />
                    </div>
                    
                    <!-- TODO: bottom actions -->

                    <!-- chatter -->
                    <div id="contract_communication" class="mt-4">
                        <h2>Communication</h2>
                        <t t-call="portal.message_thread">
                            <t t-set="object" t-value="contract"/>
                        </t>
                    </div>
                </div>

            </div>
        </xpath>
    </template>

    <!--
        This template should contains all the printable element of the Contract. This is the
        template rendered in PDF with the report engine.
    -->
    <template id="contract_content" name="Contract Content">
        <!-- Intro -->
        <div id="introduction" t-attf-class="pb-2 pt-3 #{'card-header bg-white' if report_type == 'html' else ''}">
            <h2 class="my-0">
                <span t-field="contract.code"></span>
                <em t-esc="contract.name"/>
            </h2>
        </div>

        <div t-attf-class="#{'card-body' if report_type == 'html' else ''}">
            <!-- Informations -->
            <div id="informations">
                <div class="mb-3">
                    <strong>Customer:</strong> 
                    <span t-field="contract.partner_id" t-options='{
                        "widget": "contact",
                        "fields": ["address", "name", "phone"]
                        }'/>
                </div>

                <div class="mb-3">
                    <strong>Date of Next Invoice:</strong> <span t-field="contract.recurring_next_date" t-options='{"widget": "date"}'/>
                </div>
                <div class="mb-3">
                    <strong>End Date:</strong> <span t-field="contract.date_end" t-options='{"widget": "date"}'/>
                </div>
                <div class="mb-3">
                    <strong>Payment terms:</strong> <span t-if="contract.payment_term_id" t-esc="contract.payment_term_id.name"/>
                </div>

                <t t-set="lines" t-value="[i for i in contract.contract_line_ids if i.state not in ['canceled']]"/>
                <div t-if="lines" class="row">
                    <div class="col">
                        <strong class="d-block mb-1">Contract Lines</strong>
                        <ul class="list-group mb-4">
                            <t t-foreach="lines" t-as="i">
                                <t t-set="report_url" t-value="i.get_portal_url(report_type='pdf', download=True)"/>
                                <a class="list-group-item list-group-item-action d-flex flex-wrap align-items-center justify-content-between py-2 px-3" t-att-href="report_url">
                                    <div>
                                        <i class="fa fa-download mr-1" role="img" aria-label="Download" title="Download"/>
                                        <span t-esc="i.state" class="mr-lg-3"/>
                                        <!-- <div class="small d-lg-inline-block">Date: <span class="text-muted" t-field="i.date_invoice"/></div> -->
                                    </div>
                                </a>
                            </t>
                        </ul>
                    </div>
                </div>

            </div>
        </div>

        <!-- Recurring Items -->
        <!-- <t t-call="website_portal_contract.contract_recurring_items" /> -->
        <!-- Balance -->
        <!-- <t t-call="website_portal_contract.contract_invoices" /> -->
        <!-- Descriptions -->
        <!-- <div t-field="contract.website_template_id.website_description" class="oe_no_empty" /> -->
        <!-- <div class="oe_structure" /> -->
    </template>


   

    <template id="contract_lines" name="Contract ">
        <section id="recurring_items">
            <t t-if="contract.recurring_invoice_line_ids">
                <h1 class="page-header">Recurring Invoice Items</h1>
                <table class="table wq-table">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Quantity</th>
                            <th>UoM</th>
                            <th>Subtotal</th>
                        </tr>
                    </thead>
                    <tbody>
                        <t t-foreach="contract.recurring_invoice_line_ids" t-as="invoice_line">
                            <tr>
                                <td>
                                    <span t-field="invoice_line.display_name" />
                                </td>
                                <td>
                                    <span t-field="invoice_line.quantity" />
                                </td>
                                <td>
                                    <span t-field="invoice_line.uom_id" />
                                </td>
                                <td>
                                    <span t-field="invoice_line.price_subtotal" />
                                </td>
                            </tr>
                        </t>
                    </tbody>
                </table>
            </t>
        </section>
    </template>

    <template id="contract_invoices" name="Contract Invoices">
        <section id="invoices">
            <t t-if="contract.account_invoice_ids">
                <h1 class="page-header">Invoices</h1>
                <table class="table wq-table">
                    <thead>
                        <tr>
                            <th>Invoice #</th>
                            <th>Invoice Date</th>
                            <th>Due Date</th>
                            <th></th>
                            <th></th>
                            <th>Amount Due</th>
                        </tr>
                    </thead>
                    <tbody>
                        <t t-foreach="contract.account_invoice_ids" t-as="invoice">
                            <tr>
                                <td class="cell_overflow">
                                    <span t-field="invoice.number" />
                                </td>
                                <td><span t-field="invoice.date_invoice"/></td>
                                <td><span t-field="invoice.date_due"/></td>
                                <td>
                                    <t t-if="invoice.state == 'open'">
                                        <span class="label label-info"><i class="fa fa-fw fa-clock-o"/> Waiting for Payment</span>
                                    </t>
                                    <t t-if="invoice.state == 'paid'">
                                        <span class="label label-default"><i class="fa fa-fw fa-check"/> Paid</span>
                                    </t>
                                    <t t-if="invoice.state == 'cancel'">
                                        <span class="label label-default"><i class="fa fa-fw fa-remove"/> Cancelled</span>
                                    </t>
                                </td>
                                <td>
                                <a t-if="invoice.state == 'open'"
                                    t-attf-href="/website_payment/pay?reference=#{invoice.number}&amp;amount=#{invoice.residual}&amp;currency_id=#{invoice.currency_id.id}&amp;country_id=#{invoice.partner_id.country_id.id}"
                                    alt="Pay Now" class="btn btn-xs btn-primary">
                                        <i class="fa fa-arrow-circle-right" />
                                        Pay Now
                                </a>
                                </td>
                                <td><span t-field="invoice.residual" t-options='{"widget": "monetary", "display_currency": invoice.currency_id}'/></td>
                            </tr>
                        </t>
                    </tbody>
                </table>
            </t>
        </section>
    </template>

</odoo>
